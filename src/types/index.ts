export type Settings = {
  includeTitleSlide: boolean;
  backgroundColor: string;
  textColor: string;
  fontFamily: string;
};

export type Lyrics = {
  sectionTitle: string | null;
  lyrics: string;
}[];
